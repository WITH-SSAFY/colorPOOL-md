server:
  port: 3000

spring:
  datasource:
    url: jdbc:mysql://k3a501.p.ssafy.io:3306/colorpoolmd?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: weareteam!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  version : 1.0.1
  api-docs:
    path: /api-docs
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  swagger-ui:
    operations-sorter: method
    tags-sorter: alpha
    disable-swagger-default-url: true
    display-query-params-without-oauth2: true
    path: /docs/swagger-ui.html
  paths-to-match:
    - /api/**